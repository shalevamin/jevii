# Browser tools

Chrome CDP is built into the Gateway for page automation. Prefer a dedicated CDP profile for agent work so your main browser session stays untouched.

TypeSafe powers goal decisions; CDP performs navigation and clicks.
