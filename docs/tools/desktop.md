# Desktop tools

Vision-first Mac control: see the screen, move the pointer, click, type.

TypeSafe (when configured) plans goal steps; local mouse/keyboard execute them. Accessibility is a helper when UI labels exist — not a per-app script layer.
